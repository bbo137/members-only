<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body
  style="width: 100vw; height: 100%; background-color: #f5f5f5; overflow: scroll; display: flex; flex-direction: column;">
  <div class="content d-flex flex-column align-items-center justify-content-between" style="flex: 1 0 auto;">
    <%- include(`./partials/content/${route}.ejs`) %>
  </div>
  <% if (isAuth) { %>
    <div class="footer d-flex justify-content-between align-items-center py-3 position-fixed bottom-0"
      style="padding: 0 20px; width: 100vw; background-color: #202124;">
      <p class="mb-0" style="color: #e7e5e4;">Signed as <%= user %>
      </p>
      <form action="/logout" method="GET">
        <button type="submit" class="btn btn-primary">Logout</button>
      </form>
    </div>
    <% } else { %>
      <div class="footer d-flex justify-content-between align-items-center py-3 position-fixed bottom-0"
      style="min-height: 40px; padding: 0 20px; width: 100vw; background-color: #202124;">
      </div>
      <% } %>
</body>

</html>